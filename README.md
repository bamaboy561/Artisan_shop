# Artisan

Artisan — это коммерческая платформа для продажи плитных материалов, фурнитуры и услуг распила.  
Проект собран как единый Next.js-продукт с публичным сайтом, каталогом, калькулятором распила, личным кабинетом и админкой.

## Что уже есть

- публичный сайт на русском языке;
- каталог, который наполняется через админку реальными товарами и брендами;
- страницы брендов и карточки товаров;
- онлайн-калькулятор распила;
- экспорт заявки на распил и сохранение в админку;
- CRM-карточка заявки с менеджерскими заметками, файлами результата и итоговой суммой;
- CRM-карточка заказа с планом готовности, выдачей, заметками и клиентскими комментариями;
- отдельная очередь распила для производства;
- отображение результата распила и файлов в личном кабинете клиента;
- отображение production-статуса заказа в личном кабинете клиента;
- публичная история статусов по заявкам и заказам в личном кабинете;
- дозагрузка файлов клиентом к активной заявке из личного кабинета;
- очередь заявок и заказов в админ-панели;
- demo-режим для входа в админку без живой базы;
- Telegram-уведомления для заявок, заказов и смены статусов;
- Prisma-схема под дальнейший переход на PostgreSQL.

## Технологии

- `Next.js 16` App Router
- `TypeScript`
- `Tailwind CSS 4`
- `Prisma`
- `PostgreSQL`
- `JOSE` для сессий
- `Zod` для валидации

## Структура проекта

```text
app/                маршруты public, account, admin
components/         переиспользуемые UI и layout-компоненты
features/           прикладная логика каталога, брендов, калькулятора
lib/                auth, db, server helpers, интеграции
data/imports/       временные raw-импорты, если они нужны для наполнения
prisma/             schema, optional demo seed и production bootstrap
scripts/            импорт данных брендов
styles/             тема и глобальные стили
```

## Основные разделы

- `/` — главная
- `/catalog` — каталог
- `/product/[slug]` — карточка товара
- `/calculator` — калькулятор распила
- `/services` — услуги и заявка по файлу
- `/brands` — бренды
- `/account` — личный кабинет
- `/admin` — админ-панель
- `/admin/cutting` — очередь распила и файловый мост Giblab v1
- `/admin/orders/[id]` — рабочая карточка заказа до выдачи клиенту
- `/ui-kit` — витрина компонентов

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Скопируйте пример:

```bash
copy .env.example .env
```

Минимально для локального запуска в demo-режиме нужны:

- `SESSION_SECRET`
- `ARTISAN_DEMO_MODE="true"`

### 3. Запуск разработки

```bash
npm run dev
```

Сайт откроется на:

- [http://127.0.0.1:3000](http://127.0.0.1:3000)

## Demo-доступ в админку

Если включён `ARTISAN_DEMO_MODE="true"`, можно войти без PostgreSQL:

- `Email`: `admin@artisan.local`
- `Пароль`: `Artisan123!`

Страница входа:

- [http://127.0.0.1:3000/login](http://127.0.0.1:3000/login)

Админка:

- [http://127.0.0.1:3000/admin](http://127.0.0.1:3000/admin)

## Работа с базой

Когда понадобится перейти с demo-режима на живую базу:

1. выключить `ARTISAN_DEMO_MODE`
2. указать рабочий `DATABASE_URL`
3. указать `ARTISAN_ADMIN_EMAIL` и `ARTISAN_ADMIN_PASSWORD`
4. применить схему
5. выполнить clean bootstrap без demo-каталога

Команды:

```bash
npm run prisma:generate
npm run prisma:push
npm run prisma:bootstrap
```

`npm run prisma:bootstrap` создаёт роли, первого супер-админа, базовые настройки калькулятора и способы выдачи. Он не загружает demo-товары, fake-заказы и тестовых клиентов.

Если нужен старый демонстрационный набор данных, отдельно доступен:

```bash
npm run prisma:seed
```

## Публикация в сеть

Сейчас есть два сценария:

### 1. Быстрая внешняя ссылка

Для демонстрации интерфейса можно использовать Vercel в `demo`-режиме.  
Этот режим подходит для показа сайта, но не для постоянной работы заявок и файлов.

### 2. Нормальный боевой запуск

Для реальной работы в сети рекомендуется VPS c Docker:

- `Next.js` работает как постоянный Node.js-сервис;
- `PostgreSQL` хранит реальные заявки, заказы и пользователей;
- файлы клиентов сохраняются в постоянный volume;
- админка работает без `demo`-режима.

Быстрые шаги:

```bash
cp .env.production.example .env.production
docker compose --env-file .env.production up -d --build
docker compose --env-file .env.production exec app npm run prisma:push
docker compose --env-file .env.production exec app npm run prisma:bootstrap
```

Подробная инструкция:

- [DEPLOY_VPS.md](./DEPLOY_VPS.md)

Если production-базу нужно заменить или текущий PostgreSQL упёрся в лимиты:

- [docs/PRODUCTION_DATABASE_MIGRATION.md](./docs/PRODUCTION_DATABASE_MIGRATION.md)

## Полезные команды

```bash
npm run dev
npm run lint
npm run typecheck
npm run build
```

Импорт данных:

```bash
npm run import:extravert:ldsp
npm run import:swisskrono:ldsp
npm run import:agt:panel
```

## Интеграции

### Telegram

Поддержаны уведомления:

- новая заявка;
- новая заявка на распил;
- новый заказ;
- смена статуса;
- назначение менеджера;
- готовность к выдаче.

Настройка идёт через:

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `TELEGRAM_MESSAGE_THREAD_ID`
- `TELEGRAM_REQUESTS_THREAD_ID`
- `TELEGRAM_ORDERS_THREAD_ID`
- `TELEGRAM_CUTTING_THREAD_ID`

### 1С

Каркас интеграции подготовлен, но сейчас отключён и не является обязательным для запуска.

### Giblab

В v1 используется файловый мост: клиент отправляет детали или готовый файл, менеджер готовит раскрой в Giblab, затем прикрепляет карту, ведомость, PDF/Excel/архив и итоговую сумму в карточке заявки. Видимые клиенту файлы появляются в `/account/requests`.

## Текущий статус проекта

Сейчас проект уже подходит для:

- презентации компании;
- показа реального каталога;
- приёма заявок на распил;
- ведения заявок, распила и заказов в админке;
- передачи результата распила клиенту через личный кабинет;
- контроля производства, готовности и выдачи заказа;
- показа клиенту безопасной истории изменений без внутренних заметок команды;
- приема дополнительных файлов по заявке без повторного обращения в мессенджер;
- демонстрации архитектуры будущей e-commerce платформы.

Для полноценного production-запуска ещё нужно:

- подключить живую PostgreSQL-базу;
- добить оставшиеся admin-модули на реальных данных;
- доработать личный кабинет;
- добавить финальное коммерческое наполнение;
- закрыть юридические и операционные детали.

## Roadmap

Подробный план работ лежит в [ROADMAP.md](./ROADMAP.md).
